# Scripts

Report generators validate and aggregate the committed Performance Evaluation evidence without
external dependencies. `reproduce_performance_evaluation.ps1` validates normal, benchmark,
profiling, and retained candidate builds without rerunning historical matrices.
