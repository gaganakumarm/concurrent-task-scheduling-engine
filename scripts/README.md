# Scripts

Report generators validate and aggregate the committed Phase 5 evidence without
external dependencies. `reproduce_phase5.ps1` validates normal, benchmark,
profiling, and retained candidate builds without rerunning historical matrices.
